# machine’s current git version

git --version

# Set a Username & Email

git config --list 

git config --global user.name "soham"
git config --global user.email "hello@zzz.com"

#create a new Git repository in a new or existing project

git init

#View the current status of a repository.

git commit status

